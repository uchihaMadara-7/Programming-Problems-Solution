#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void multiple_of_3(vector<int> array, int n)
{
	sort(array.begin(), array.end());	
	for(int i=0;i<n;i++)
		sum += array[i];

	if(sum % 3 == 0)
		return;
	for(int i=0;i<n;i++)
	{
		sum -= array[i]
		if()
	}
}

int main()
{
	int n;
	cin >> n;

	vector<int> array(n);
	for(int i=0;i<n;i++)
		cin >> array[i];
}